[pytest]
testpaths = tests
# Les modules testés vivent dans le démon : on les rend importables sans
# packaging (le plugin n'est pas distribué comme une bibliothèque).
pythonpath = resources/bambujabd
